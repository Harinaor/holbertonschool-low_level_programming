#include ?

/** 
 * main - appel des fonctions des opérations 
 * contenu dans op_functions.c
 * return ?
 */
Ce fichier devrait contenir ta fonction main seulement.

void main(void)
{
    int index;

    index = int_index(array, 20, is_98);
    printf("%d\n", index);
Appel indirect des opérations :
op_add
op_sub
op_mul
op_div
op_mod
atoi doit être utilisé pour convertir les arguments en entier.
3 if maximum et rien d'autre
}
