#include "main.h"

/**
 * mul - ecrire une fonction qui multiplie deux entiers.
 * @a: le premier entier
 * @b: le deuxieme entier
 * Return: le resultat de la multiplication de a et b
 */
int mul(int a, int b)
{
	return (a * b);
}
