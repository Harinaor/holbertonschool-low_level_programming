#include "main.h"

/**
 * puts_half - affiche la moitié d'une chaîne,
 * suivi d'une nouvelle ligne 
 * Return: affiche la seconde moitié de la chaîne
 */

void puts_half(char *str);
/* Si le nombre de caractères est étrange, 
 * la fonction devrait afficher les derniers n caractères de la chaîne
 * où n = (longueur de la chaine + 1) / 2 */
 