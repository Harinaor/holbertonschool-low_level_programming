/**
 * Ecrire une fonction qui prend un pointeur
 * vers un entier comme paramètre 
 * et met à jour la valeur pointée par ce pointeur à 98. 
 *Return: valeur pointée à 98
*/
void reset_to_98(int *n);
{
int paquet
int contenu

*paquet = &contenu;
*paquet = 98; 
return(paquet);
}