#include <stdio.h>

/**
 * main -Entry point
 * 
 * printf afficher sans retour a la ligne inclus
 * return arr√te le programme proprement
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
